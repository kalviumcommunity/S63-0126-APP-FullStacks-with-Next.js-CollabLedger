# Environment Variables Configuration
# =====================================
# This file serves as a template for all required environment variables.
# Copy this file to create .env.local in your local development environment.
# 
# To get started:
# 1. Copy this file: cp .env.example .env.local
# 2. Fill in the placeholder values with your actual configuration
# 3. Never commit .env.local to version control

# ============================================
# SERVER-SIDE VARIABLES
# ============================================
# These variables are only available on the server (API routes, server components).
# They are NOT accessible in the browser and should be used for sensitive data.
# Do NOT prefix these with NEXT_PUBLIC_

# Database Connection String
# Format: postgresql://username:password@host:port/database
# Used by Prisma ORM for database operations
DATABASE_URL=postgresql://username:password@localhost:5432/collabledger_db

# API Secret Keys (example)
# Used for authenticating backend services
# EXAMPLE_API_SECRET=your_secret_key_here

# ============================================
# CLIENT-SIDE VARIABLES (PUBLIC)
# ============================================
# These variables are accessible in both server and client code.
# They ARE exposed to the browser, so NEVER put sensitive data here.
# MUST be prefixed with NEXT_PUBLIC_ to be accessible in the browser.

# Public API Base URL
# Used for making API calls from both server and client components
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000/api

# Public Analytics Tracking ID (example)
# Used for client-side analytics
# EXAMPLE: NEXT_PUBLIC_ANALYTICS_ID=UA-XXXXXXXXX-X
